# Required environment variables for Account Deletion Notification Service
# Do not share this file — use it as a reference only

# Verification token for webhook security
VERIFICATION_TOKEN=your_verification_token_here

# Port for the service to run on
PORT=3000

# Optional: Environment name (development, production)
NODE_ENV=development 